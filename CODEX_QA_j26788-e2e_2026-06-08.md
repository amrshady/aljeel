Implemented and verified. No deploy was run.

**Findings**
- Issue 1 root cause: `droplet_api_flask.py` accepted `invoice_path` from `/process` and passed portal uploads as `--invoice-file`. Fixed. `/upload` still stores Excel files, but uploads are now display/comparison artifacts only.
- Issue 2 root cause: the lookup workbook used by code is `qc/master-data/Aljeel_Lookups-v2.xlsx`, and it exists, but it has no `Cost Center` or `Cost Center Segment` sheet. The resolver expects either of those sheet names. Warning still appears because the sheet is genuinely missing.
- Requested root lookup file `/home/clawdbot/.openclaw/workspace/aljeel/Aljeel_Lookups-v2.xlsx` is missing. Existing copies are under `qc/master-data/` and `dashboard/public/files/`.
- Issue 3 root cause: Stage 1/Stage 2 filename handoff was correct, but Stage 2 evidence scanning was too narrow. `run_v30.py` only scanned live `raw` with legacy depth assumptions plus direct `gdrive-evidence`; J26-788 evidence also lives under archive/direct/nested folder shapes. Fixed Stage 2 evidence folder collection.
- Additional issue found: Stage 1 preferred stale generated `output/Spreadsheet-*-FILLED-v4*.xlsx` over canonical `Spreadsheet-v4-input.xlsx`. Fixed to prefer `Spreadsheet-v4-input.xlsx`.

**Files Changed**
- [scripts/droplet_api_flask.py](/home/clawdbot/.openclaw/workspace/aljeel/scripts/droplet_api_flask.py:106)
- [scripts/process_batch.py](/home/clawdbot/.openclaw/workspace/aljeel/scripts/process_batch.py:561)
- [scripts/run_v30.py](/home/clawdbot/.openclaw/workspace/aljeel/scripts/run_v30.py:802)

**Diff Summary**
```diff
droplet_api_flask.py
- removed invoice_path query param handling from /process
- removed _is_under_uploads_dir helper used only for invoice_path validation
- removed cmd1 += ["--invoice-file", uploaded_invoice]
- changed /upload docstring to display/comparison use
+ Stage 1 SSE now states portal invoice upload is display-only

process_batch.py
- preferred output/Spreadsheet-*-FILLED-v4*.xlsx before batch input
+ now prefers Spreadsheet-v4-input.xlsx, then Spreadsheet.xlsx, then legacy v4 output fallback

run_v30.py
+ added _collect_evidence_folders(batch_id, batch_dir)
+ scans batch raw, gdrive-evidence, gdrive-evidence2, and archive/raw-<batch>
+ recursively includes folders containing .msg/.pdf/.eml
- replaced narrow raw/gdrive folder scan with the new collector
```

**Input Checklist**
- Root master lookups: `Aljeel_Lookups-v2.xlsx` at workspace root: MISSING.
- Code default master lookups: `qc/master-data/Aljeel_Lookups-v2.xlsx`: EXISTS. Sheets: `Account`, `Agency`, `Solution`, `DIV`, `Manpower`.
- Dashboard copy: `dashboard/public/files/Aljeel_Lookups-v2.xlsx`: EXISTS. Same sheets.
- Missing lookup sheet: expected `Cost Center` or `Cost Center Segment`.
- Spreadsheet input: `batches/jawal-J26-788/Spreadsheet-v4-input.xlsx`: EXISTS. Sheet: `Sheet1`.
- Raw evidence: `batches/jawal-J26-788/raw`: EXISTS as symlink to archive path. Following symlink: 91 `.msg` files, 0 `.msg` directories.
- Stage 1 output logic: writes `batches/jawal-J26-788/output/Spreadsheet-J26-788-FILLED-<suffix>.xlsx`.
- Stage 2 input logic: `run_v30.py J26-788 --input-suffix v15.11.2` looks for `Spreadsheet-J26-788-FILLED-v15.11.2.xlsx`. This matches Flask Stage 1 suffix.
- Stage 2 script: `scripts/run_v30.py`: EXISTS.
- Other xlsx referenced by Stage 1: `qc/master-data/Employee_Email_Master_2026-05-25.xlsx` is referenced but MISSING; code falls back to learned email cache. `LineManagerOverrides.xlsx` is referenced by employee resolver and EXISTS.

**Dry Run**
Command run as Stage 1 only, no `run_v30.py`:
```bash
python3 scripts/process_batch.py --batch batches/jawal-J26-788 --raw-dir batches/jawal-J26-788/raw --suffix v_qa_test --no-cache
```

Result:
- Exit status: `0`
- Input used: `Spreadsheet-v4-input.xlsx`
- Output written: `batches/jawal-J26-788/output/Spreadsheet-J26-788-FILLED-v_qa_test.xlsx`
- Rows: `103`
- CC resolver warning still present because lookup sheet is missing:
  `Cost Center segment master sheet missing; falling back purely to Manpower CC entries.`

Syntax check passed:
```bash
python3 -m py_compile scripts/droplet_api_flask.py scripts/process_batch.py scripts/run_v30.py
```
