Implemented and did not deploy.

**Diff Summary**
Touched source:
- [asateel_poc.py](/home/clawdbot/.openclaw/workspace/aljeel/asateel-sample/asateel_poc.py:872)

No source changes:
- [pipelines/asateel.py](/home/clawdbot/.openclaw/workspace/aljeel/pipelines/asateel.py)

Generated by validation run:
- `matched/asateel-oracle-upload.xlsx`
- `matched/asateel-allocation.json`
- `matched/asateel-catch.json`
- `matched/asateel-summary.json`
- `matched/asateel-trace.json`
- `extracted/asateel-pdf-headers.json`

Source diff was 184 unified-diff lines. Main changes:
- Added `_sperson_id()` parsing leading numeric salesperson ids.
- Changed SO_Detail allocation units from distinct `CAT_AGENCY` only to distinct `(CAT_AGENCY, SPERSON_ID)`.
- Kept org-only differences non-splitting.
- Added split method selection:
  - `per_jq_agency_even`
  - `per_jq_sperson_even`
  - `per_jq_agency_sperson_even`
- Added tuple/salesperson split notes.
- Added executable self-check flag: `--self-check-so-detail-sperson-split`.

**Before / After CENTRAL**
Before expected from last run:
- Invoices: `92`
- Rows: `204`
- GREEN/YELLOW/RED: `96/104/4`
- Split methods: `even:6`, `n/a:3`, `per_jq:160`, `per_jq_agency_even:31`, `per_line:4`

After `python3 pipelines/asateel.py --folder CENTRAL --full`:
- Invoices: `92`
- Rows: `204`
- GREEN/YELLOW/RED: `96/104/4`
- Split methods: `{'even': 6, 'n/a': 3, 'per_jq': 160, 'per_jq_agency_even': 31, 'per_line': 4}`
- Reconciled/mismatched: `88/4`
- Header validation: `A:AF match = True`, diffs `[]`

Confirmed dormant on current SO_Detail export:
- Loaded JQs: `9552`
- Multi-agency: `827`
- Same-agency/different-org: `365`
- Same-agency/multi-salesperson: `0`
- Multi-agency/multi-salesperson: `0`

**Synthetic Proof**
Ran:

```bash
python3 asateel-sample/asateel_poc.py --self-check-so-detail-sperson-split
```

Output proves same agency plus two salespersons splits:

```json
{
  "split_method": "per_jq_sperson_even",
  "amounts": [50.0, 50.01],
  "note": "JQ split across 2 salespersons on agency 10202/Solventum: 1000473-Abdallah A, 1001031-Mahmoud S; transport cost split evenly"
}
```

Validation passed:
- `python3 -m py_compile asateel-sample/asateel_poc.py pipelines/asateel.py`
- `python3 pipelines/asateel.py --folder CENTRAL --full`
